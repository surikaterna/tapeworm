{
  "name": "tapeworm-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Tapeworm event sourcing monorepo",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@8.19.2",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "check": "turbo run check"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
